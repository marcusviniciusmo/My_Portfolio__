export interface BlogType {
  id: string;
  name: string;
  image: string;
  url: string;
}

export interface BlogProps {
  $borderColor: string;
  $isListInHover: boolean;
  $isItemInHover: boolean;
}
